import numpy as np


class SeamCarver:

    def compute(self, image: np.array) -> np.array:
        return np.zeros(100)

    def remove(self, image: np.array) -> np.array:
        return np.zeros(100)
